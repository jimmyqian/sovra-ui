# Basic Authentication Configuration
# Set these variables to enable basic auth for the dev server
# Auth is automatically disabled if these are not set or if VITE_DISABLE_AUTH=true

# Username for basic auth
VITE_AUTH_USER=

# Password for basic auth
VITE_AUTH_PASS=

# Uncomment to completely disable auth (useful for local dev)
# VITE_DISABLE_AUTH=true
